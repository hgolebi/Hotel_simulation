#include <iostream>
#include <vector>
#include "hotel.h"
#include "room.h"

Hotel(std::string name, std::vector<*Room> rooms, std::vector<*Worker> workers, std::vector<*Guests> guests, double capital);
{
    this-> name = name;
    this-> rooms = rooms;
    this-> workers = workers;
    this-> guests = guests;
    this-> capital = capital;
}